//! arena-server: the headless authoritative server. Fleshed out by Task 7 (boots + listens) and
//! later tasks (obelisk sim, rounds, egress). M2.1 Task 5 starts it as a minimal stub so the crate
//! splits into lib + 3 bins and builds.

fn main() {
    eprintln!("arena-server: stub (Task 7 wires the net + obelisk stack)");
}
