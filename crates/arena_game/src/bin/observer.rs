//! arena-observer: the scripted headless trace client (M2.5 Task 20). Started as a stub here so the
//! crate's three-bin layout is in place from Task 5.

fn main() {
    eprintln!("arena-observer: stub (Task 20 wires the scripted net trace client)");
}
