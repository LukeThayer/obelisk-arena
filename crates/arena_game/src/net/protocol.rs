//! Protocol: identical on every peer. Filled in by Task 6.
use bevy::prelude::*;

pub struct ProtocolPlugin;

impl Plugin for ProtocolPlugin {
    fn build(&self, _app: &mut App) {
        // Task 6 registers components + channels + messages here.
    }
}
