//! Client-side network plugin. Filled in by Task 8.
use bevy::prelude::*;

pub struct ClientNetPlugin;

impl Plugin for ClientNetPlugin {
    fn build(&self, _app: &mut App) {
        // Task 8 adds ClientPlugins + ProtocolPlugin + connect setup here.
    }
}
