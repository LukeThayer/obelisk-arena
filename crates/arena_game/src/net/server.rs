//! Server-side network plugin. Filled in by Task 7.
use bevy::prelude::*;

pub struct ServerNetPlugin;

impl Plugin for ServerNetPlugin {
    fn build(&self, _app: &mut App) {
        // Task 7 adds ServerPlugins + ProtocolPlugin + listen setup here.
    }
}
